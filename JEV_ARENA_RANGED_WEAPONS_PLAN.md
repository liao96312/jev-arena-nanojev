# Jev Arena 玩家远程武器规划

> 状态：设计完成，尚未接入代码。  
> 目标：主角可从地面拾取弓或脉冲手枪，获得有限次数的远程攻击；武器所有权与剩余弹药跨关、重开游戏后继续保留。

## 1. 核心结论

当前射击只属于 Archer 敌人。玩家远程能力应作为“可拾取的持久装备”加入，而不是直接增加一个永久免费动作。

第一版只做两种武器：

| 武器 | 初始弹药 | 射程 | 伤害 | 战术特点 |
| --- | ---: | ---: | ---: | --- |
| 复合弓 | 3 | 6 格 | 15 | 命中后击退 1 格，可把敌人推入火焰或撞向其它敌人 |
| 脉冲手枪 | 6 | 8 格 | 12 | 射程长、弹药多，不击退 |

共同规则：只允许上下左右射击；墙会阻挡；只命中射线上的第一个敌人；每次射击消耗 1 发并消耗当前回合。

## 2. 拾取与持久化

- 玩家踩到武器所在格时自动拾取，不额外消耗动作。
- 第一次拾取会永久解锁该武器，并获得对应初始弹药。
- 重复拾取同一种武器时转换成补给：弓 `+3` 箭，手枪 `+6` 能量。
- 独立弹药拾取物：箭束 `+3`，能量弹匣 `+6`。
- 武器即使弹药归零也不会消失；之后拾到弹药即可再次使用。
- 建议上限：箭 12、能量 24，避免无限囤积让近战和地形失去意义。
- 武器、弹药在过关、死亡重试和关闭游戏后保留；只有明确选择“新建战役/清除存档”才重置。
- 本地存档使用版本化 JSON，并采用临时文件后原子替换，损坏时回退到空背包而不影响游戏启动。

## 3. 地图投放

- 第 1 关若尚未拥有弓，保证生成 1 把复合弓。
- 第 3 关若尚未拥有手枪，保证生成 1 把脉冲手枪。
- 已解锁武器后，每关按确定性 seed 生成 0~2 个对应弹药拾取物。
- 武器与弹药必须位于玩家可到达区域，不能占用墙、火焰、敌人或任务宝石格。
- 难度提升优先降低补给频率，不提高玩家远程伤害。

## 4. 动作与候选控制

新增动作：

```text
shoot_bow_n / shoot_bow_s / shoot_bow_w / shoot_bow_e
shoot_pistol_n / shoot_pistol_s / shoot_pistol_w / shoot_pistol_e
```

为避免候选从 6~10 个膨胀到 18 个，射击动作只在以下条件全部满足时出现：

1. 已解锁对应武器；
2. 剩余弹药大于 0；
3. 对应方向射程内存在未被墙挡住的敌人。

如果同一方向两种武器都可用，则保留两个候选，让 NanoJev 自己权衡伤害、击退和弹药成本。

候选示例：

```text
shoot_bow_e: Hit bomber E/4 hp30->15; push 1; arrows 3->2
shoot_pistol_e: Hit bomber E/4 hp30->18; energy 6->5
```

空射第一版不提供，因为没有战术收益，只会增加无效分支。

## 5. 状态与显示

Observation 使用紧凑字段，避免破坏现有 `max_length=192`：

```text
Inv bow=3 pistol=6. Near pickup=arrows NW/4.
```

GUI 增加：

- 地面武器和弹药的独立贴图；
- 右侧状态栏显示已解锁武器与剩余次数；
- 箭矢飞行、脉冲弹道、命中闪光和击退动画；
- 中文事件文字：`拾取复合弓`、`箭矢剩余 2`、`能量弹药耗尽`。

Replay 必须保存武器所有权、弹药、地面拾取物、射击动作和命中目标，保证完全复现。

## 6. 数据结构边界

地图状态和跨关状态分开：

- `ArenaEnv`：本关地面武器、弹药拾取物与战斗解析；
- `PlayerLoadout`：已解锁武器、当前弹药；
- `CampaignSave`：关卡、累计分数、`PlayerLoadout` 和存档版本。

不要在第一版加入装备品质、随机词条、换弹回合、八方向瞄准或复杂背包格子。这些会扩大 Candidate、Observation 和 UI，但不会立刻增加有效博弈。

## 7. 实施顺序

### 阶段 A：持久背包

- 增加武器类型、玩家 Loadout 和版本化存档。
- 关卡切换时显式传递 Loadout。
- 添加跨关、死亡重试和重新启动后的持久化测试。

### 阶段 B：拾取与射击规则

- 加入四类地面拾取物。
- 抽取玩家与 Archer 共用的直线射线检测。
- 实现弓击退和手枪命中。
- 增加墙阻挡、首个目标、弹药消耗、击退入火/撞怪测试。

### 阶段 C：Observation / Candidate / Agent

- 输出紧凑库存与最近拾取物。
- 动态生成有效射击候选，并由现有一回合模拟描述立即后果。
- 让 RuleAgentV2 计入弹药成本、击杀、环境击杀与脱险收益。
- 重新执行候选数量、死局率和 `max_length=192` token 审计。

### 阶段 D：中文 GUI 与 Replay

- 接入四张新贴图和投射物动画。
- 增加中文 HUD、事件反馈和弹药耗尽提示。
- 扩展 Replay 后做确定性回放测试。

### 阶段 E：平衡验收

- 100 seed 对比无武器/有武器的通关率、奖励和环境击杀率。
- 射击候选出现时，至少 80% 的状态仍保留两个以上合理应对动作。
- 典型候选数量维持 6~10，最大不超过 12。
- 不允许远程武器让近战、Shove、Dash 或火焰玩法失去价值。

## 8. 验收场景

1. 玩家拾到弓后过关，下一关仍有弓和剩余箭数。
2. 弹药归零后武器仍在背包，拾到箭束后重新可用。
3. 弓箭击退敌人进入火焰并产生环境击杀。
4. 敌人挡在射线上时，后方目标不会被手枪命中。
5. 墙后敌人不产生射击候选。
6. 同方向弓和手枪都能命中时，两种候选同时出现且描述准确。
7. 关闭并重启游戏后，武器与弹药从本地存档恢复。
8. Replay 可复现拾取、射击、弹药变化和击退结果。

## 9. 已准备素材

```text
assets/sprites/item_bow.png
assets/sprites/item_pulse_pistol.png
assets/sprites/ammo_arrows.png
assets/sprites/ammo_energy_cell.png
```

四张素材均由 ImageGen 生成，为 1254×1254 RGBA 透明 PNG；尚未注册进 Renderer，也未修改任何游戏逻辑。
